# Core dependencies for the Music Lyrics & Theme Analyzer Bot

# Official Python client for the Genius API (lyrics fetching)
lyricsgenius>=3.0.0

# OpenAI-compatible SDK — used to call NVIDIA NIM endpoints
openai>=1.0.0

# Loads environment variables from .env file into os.environ
python-dotenv>=1.0.0

# Web server dependencies
fastapi>=0.100.0
uvicorn>=0.20.0
